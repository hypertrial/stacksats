name: docs-check

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install docs dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install mkdocs mkdocs-material mkdocstrings[python] codespell pymarkdownlnt

      - name: Lint markdown
        run: pymarkdown scan --disable-rules MD013,MD033 docs README.md CONTRIBUTING.md CHANGELOG.md

      - name: Check spelling
        run: codespell docs README.md CONTRIBUTING.md CHANGELOG.md --config .codespellrc

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --glob --verbose --no-progress --max-retries 2 --retry-wait-time 2
            --accept 200,429 --exclude-mail
            docs/**/*.md README.md CONTRIBUTING.md CHANGELOG.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check docs references
        run: bash scripts/check_docs_refs.sh

      - name: Build docs (strict)
        run: mkdocs build --strict
